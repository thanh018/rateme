<% layout('layout') %>

<div class="container">
  <div class="text-center">
    <h1 class="mainTitle"><a href="/">RateMe</a></h1>
    <h2 class="title">Login</h2>
  </div>

  <div class="row">
    <div class="col-md-4 offset-md-4">
      <form method="POST" action="/login">
        <div class="form-group">
          <input
            id="email"
            class="form-control"
            name="email"
            type="text"
            autocomplete="on"
            placeholder="Email"
          />
        </div>
  
        <div class="form-group">
          <input
            id="password"
            class="form-control"
            name="password"
            type="password"
            placeholder="Password"
          />
        </div>
  
        <div class="form-group">
          <input
            type="submit"
            id="login"
            value="Login"
            class="form-control btn btn-primary"
            name="login"
          />
        </div>
  
        <div class="form-group-checkbox">
          <label for="">
            <input
              type="checkbox"
              name="rememberme"
              class=""
              value="yes"
            />
          </label>
          <span>&nbsp;&nbsp;Remember me</span>
        </div>
      </form>
      <div class="form-group">
        <a
          href="/auth/facebook"
          type="button"
          class="btn btn-facebook"
        >
          <i class="fa fa-facebook"></i>
          <span>Login with Facebook</span>
        </a>
      </div>
      <div>
        <h4 class="subText">
          Don't have an account? <a href="/signup">Sign Up</a>
        </h4>
        <h4 class="subText"><a href="/forgot">Forgot Your Password</a></h4>
      </div>
    </div>
    <div class="col-md-4 errors">
      <% if(hasErrors) {%> 
        <% for(var i = 0; i < messages.length; i++) {%>
          <div class="alert alert-danger">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">
              &times;
            </a>
            <%= messages[i] %>
          </div>
        <% } %> 
      <% } %>
    </div>
  </div>
</div>
